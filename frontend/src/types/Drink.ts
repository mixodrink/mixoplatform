export type Alcohol = {
  type: string;
  alcohol: [string, string, string, string];
};

export type Soda = {
  type: string;
  soda: [string, string, string, string, string];
};

export type Water = {
  type: string;
  water: string[];
};
