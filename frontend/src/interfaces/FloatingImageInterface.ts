export interface FloatingImageInterface {
  type: string;
  selectedImage: string;
  currentStep: number;
  positionTop: [number, number, number];
  positionRight: [number, number];
  rotationDeg: number;
}
